// src/theme.js
import {
   
    createTheme,
   
} from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#f50057',
        },
    },
});

export default theme;
